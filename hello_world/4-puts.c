#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description; this is a simple program that prints a message
 * using the puts function.
 *
 * Return value for main is always 0 (success)
 */
int main(void)
{
	/** 
	 * This code prints a message using the puts function.
	 */

	puts("\"Programming is like building a multilingual puzzle");

	/** 
	 * Return value is always 0 signifying success.
	 */

	return (0);
}
