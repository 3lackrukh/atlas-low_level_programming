#include "main.h"

/**
 * leet- encodes a string into 1337.
 *
 * @p: pointer to the character string to be encoded.
 *
 * Return: the pointer (p).
 */

char *leet(char *p)
{
	int i;
	char array [5][3] = {a, A, 4} , {e, E, 3} , {o, O, 0} , {t, T, 7} , {l, L, 1};

	While (p[i] != '\0')

