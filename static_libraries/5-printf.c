#include <stdio.h>

/**
 * main - entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/**
	 * This program prints text using the print f function.
	 */

	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
